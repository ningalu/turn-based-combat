#ifndef TBC_USEREFFECT_HPP
#define TBC_USEREFFECT_HPP

#include "tbc/Slot.h"
#include "tbc/Target.h"

namespace ngl::tbc {
template <typename TBattle>
class UserEffect {
  // virtual void Apply(Slot::Index user, TBattle &battle, const std::vector<Target> &targets) = 0;
  // virtual ~Effect();
};
} // namespace ngl::tbc

#endif